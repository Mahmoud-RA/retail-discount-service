spring:
  mongodb:
    uri: ${MONGODB_URI}

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    com.d360.retailDiscountService: INFO
    org.springframework.data.mongodb.core.MongoTemplate: INFO